.message
  .upper-message
    .upper-message__user-name
      = message.user.name
    .upper-message__date
      = message.created_at.strftime("%Y年%m月%d日 %H:%M")
  .lower-meesage
    - if message.content.present?
      %p.lower-message__content
        = message.content
    - if message.image.present?
      .lower-message__image
        = image_tag(message.image.url,size: 256)
